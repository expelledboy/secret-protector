# Project-level overrides for secret-protector
# Files: globs/regex block; allow_globs/allow_regex permit (like .gitignore)
env:
  exact:
    - STRIPE_SECRET_KEY
files:
  globs:
    - '**/*production*.tfvars'
    - '**/*.kubeconfig'
  allow_globs:
    - 'config/env.example.yml'
